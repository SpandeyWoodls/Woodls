---
summary: "CLI reference for `woodls uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `woodls uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
woodls uninstall
woodls uninstall --all --yes
woodls uninstall --dry-run
```
